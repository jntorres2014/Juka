<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Se elimina el acceso de FileProvider a filesDir. Se mantiene cacheDir
         porque la cámara de Identificar Captura todavía crea temporales allí. -->
    <cache-path name="temporary_cache" path="." />
</paths>